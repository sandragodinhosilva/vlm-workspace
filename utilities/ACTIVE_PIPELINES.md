> Generated by /digest on 2026-09-07 from auto-memory — DO NOT EDIT BY HAND.
> Disposable view of ~/.claude/projects/-home-sgsilva/memory/. Re-run /digest to refresh.

# Active pipelines

⏸️ **VLM is MOTHBALLED since 2026-07-27** (Sandra on 3WC for months). Resume-from-here:
`reports/post_training/2026-07-27_VLM_MOTHBALL_resume_from_here.md`. VLM rows below are frozen
state, not live work. 3WC = `/home/sgsilva/dawn-research` (own `AGENTS.md` + `.knowledge/`).

| Pipeline | State | Repo | Entry script | Venv | Latest decision |
|---|---|---|---|---|---|
| **reasonings — W1 error-cut** | ⏳ **IN FLIGHT 2026-09-07 (session on worker-31) — three things launched, none read.** (1) W1 error-cut jobs `154923` qwen27 (primary) + `154924` qwen9, 3,279 cells each on worker-2 — ⚠️ receivers diverge on `hit_cap`: qwen27 8.6% (below its 12.2% baseline, primary SAFE) vs qwen9 45.5% and climbing ⇒ expect ~1,800 usable, NOT 3,279; ⛔ do NOT raise `max_trace_tokens` mid-run (changes `config_hash`). (2) gemma12 NODES, 6 shards detached on worker-31; edges deliberately NOT run. (3) GLM-5.3 server `154989` on **worker-29** — read the endpoint from `utilities/serve/glm53/run/endpoint.env`, never hardcode. Gate 1 DONE: cond-1 alive on nemotron4; Monday = W1 mid-error transfer, nemotron4 sources. | reasonings | `build_wiagr_input.py` (needs **rteval** venv) | reasoningflow / rteval | [[project_conclusion_strip_label_as_is_decision]] |
| **3WC — medication screen** | SCREEN COMPLETE 2026-09-05 (150 rollouts / 15 pts, 21% flagged); at the gate, nothing running. 09-07: `k_flagged` stable across judges/temps (`k` is not), multi-message mutator + v1 lever landed, pain-seeded pool 1,689 pts / 1,054 convs built, pilot screening. 300 distinct convs UNREACHABLE (~13 max) ⇒ route on `k_flagged`. Code promoted out of `_campaign/` 09-07. | dawn-research | `3wc/failure_scenarios/failure_modes/screen_elicit/` | 3wc | [[project_medcheck_verifier_is_glm_decision]] |
| **3WC — af6 merge sweep / serving quant** | Sweeps DONE + boarded. FP8 clean (85.8) = the serving build; ⛔ NVFP4 triple-confirmed regression; lm_head-BF16 recovery REFUTED. FP8-KV × spec root-caused to a TP=8 PREFILL perturbation FP8 amplifies (mitigation = spec k=3 + `--kv-cache-dtype bfloat16`). SGLang second-engine validation clean (Pass^10 85.8). ⛔ In-domain calibration does NOT move W4A4 quality (Task 2 closed, clean negative). 🟡 Deferred: calib3wc × FP8-KV arm. | dawn-research | `export_quant_results.py` · `serve_sglang.sbatch` | 3wc | [[project_phoenix_serving_precision_fp8_decision]] |
| **3WC — appliedAI repo tracker** | BUILT 2026-08-28. Mirror + crawler → `registry.json` feeds `appliedai_comparison_csv.py --reported` + the **Applied AI timeline** tab on port 7881. Refresh via `appliedai_tracker.sh refresh` (swapped only on success). Mirror push URL is `DISABLED_never_push`. | dawn-research | `board/tracker/appliedai_repo_tracker.py` | 3wc | [[project_3wc_share_to_sword_flow_decision]] |
| **3WC — sendgate_judged mirror** | ✅ COMPLETE 2026-08-31. 111,717 rows dual-judged (Kimi-K3 + GLM-5.2), κ 0.80–0.84, judges overrule Phoenix on ~7–8%. Mirror at `/mnt/data/shared/3wc/datasets/sendgate_judged/` (55 GB). Servers cancelled, GPU nodes free. | dawn-research | — (complete) | 3wc | [[project_medcheck_verifier_is_glm_decision]] |
| **serve-monitor app (7878)** | Live. **Bug-fix pass 2026-09-07 (6 fixes, runtime-verified):** crash on a just-started server (`ttft` None interpolated raw); picking a scanned server with an empty scan-time model list silently cleared the model id; gradio 6 moved `theme=` off `Blocks` to `launch()` (was silently ignored). Left alone deliberately: dead `thinking_hint` var, per-refresh SSH stacking. ⚠️ `grpo_dashboard.py` has the same gradio-6 theme issue, unfixed. | utilities | `~/utilities/apps/serve_monitor.py` | vlm-post-training-home-venv | — |
| **/clean skill + checkpoint prune** | 2026-09-04: 258 GB; mode A EXHAUSTED, mode C is the reclaim. `cleanup_checkpoints.py` now reads the LIVE eval board for best-ckpt detection; export-verified guard added ([[feedback_export_before_evict]]). | utilities | `~/utilities/cleanup/cleanup_checkpoints.py` | — | — |
| **Eval master — V2 era** (VLM) | ⏸️ Frozen at VLM mothball 2026-07-27. Compacted 09-04; full chronology in `project_eval_v2_era_history_20260904`. | vlm-post-training | `compile_eval_results.py` | vlm-post-training-home-venv | [[project_eval_master_family_column_decision]] |
| **MTP head training** (3WC) | ⏳ NO-GO recommended, UNRATIFIED. Gate PASSED 0.871, but 3–4 GPU-days for +4–6% decode-only on a 270:1 prefill workload ⇒ profile spec-path overhead instead. | dawn-research | — | 3wc | [[project_mtp_head_training_nogo_decision]] |
| **VLM — visual-obs / VObs-as-TOOL** | ⏸️ Mothballed. EXP-B CLOSED (GT-obs 76.97 / model-obs 45.1 FAIL); live line was VObs-as-TOOL at ⏳ Gate-2 when work stopped. | vlm-post-training | `evaluate.py` | vlm-post-training-home-venv | [[project_vobs_tool_gt_repair_laundering_decision]] |
| **VLM — MCQA / mixes** | ⏸️ Mothballed. Canonical 12k mix = 1506; mix_2906 BUILT + canonical (13,900/652) and will supersede. ⚠️ STANDING TODO: fix ALL aux-task TEST sets (1505-test pending). | vlm-post-training | `data_preparation/` | vlm-post-training-home-venv | [[project_mix_2906_build_decisions]] |

## Recently closed / superseded
- **medcheck human validation (0c(a)/0c(b))** — closed unfilled; the screen's 62%-disagreement measurement replaces them ([[project_medcheck_human_validation_superseded_decision]]).
- **`render_speed_tables.py`** — superseded by `export_quant_results.py --card-tables` (archived under the campaign `_superseded/`).
- **VLM stage-2 native reasoner (EXP-B)** — CLOSED; superseded by the VObs-as-TOOL line ([[project_stage2_native_reasoner_thread]]).
- **task3a** — ⛔ DO NOT USE (degenerate always-true) ([[project_task3a_degenerate_always_true_decision]]).
- **video-sft-vlm** — archived ([[project_video_sft_vlm_archive]]).
